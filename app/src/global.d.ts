/// <reference types="svelte" />

export type DashboardParamsType = {
  workspaceId: string
  title: string
}

export type DocumentParamsType = {
  workspaceId: string
  uid: string
  location: string
  title: string
}
