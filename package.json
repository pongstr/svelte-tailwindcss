{
  "name": "svelte-ts",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "keywords": [],
  "author": "Pongstr <twiz.tickler@gmail.com>",
  "license": "ISC",
  "scripts": {
    "start": "yarn clean && yarn build:workers && yarn workspace @codecrumble/app dev",
    "build": "yarn build:workers; yarn build:app",
    "build:app": "yarn workspace @codecrumble/app build",
    "build:workers": "yarn workspace @codecrumble/workers build",
    "clean": "rm -rf app/public/app/main app/public/app/worker",
    "deploy": "yarn clean && yarn build && npx firebase deploy --only hosting:codecrumb;"
  },
  "dependencies": {},
  "devDependencies": {},
  "workspaces": [
    "./app",
    "./worker",
    "./electron"
  ],
  "engines": {
    "node": "16",
    "yarn": "1.22"
  },
  "resolutions": {
    "bl": "^1.2.3",
    "node-fetch": "^2.6.7"
  }
}
